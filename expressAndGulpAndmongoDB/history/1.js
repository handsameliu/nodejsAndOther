var gulp = require('gulp');
var util = require('util');
//定义一个复制文件的任务
//把src-indexx.html拷贝
gulp.task('default'),function(){
    //src获取指定文件的可读流
    //dest的参数是一个目录名，而不是一个文件名
    gulp.src(gulp.dest('./build,pipe(gulp.dest("./build*)')); //通过管道的方式注入到可写流
};



